services:
  backend:
    build: ./backend
    environment:
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET majburiy (.env fayliga qo'shing)}
    volumes:
      - db_data:/app/data
    ports:
      - "8021:8000"
    restart: unless-stopped

volumes:
  db_data:
